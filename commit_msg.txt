fix: correct mel spectrogram computation to match Whisper reference

- Use power spectrum (squared magnitudes) instead of raw magnitudes
- Change log base from natural log (ln) to log10
- Add Whisper normalization: clamp to max-8.0, scale with (val+4)/4
- Pre-pad audio to 30 seconds before STFT (matching Python Whisper)
- Fix padding value in mel domain from 1e-10 to 0.0 (silence)
- Return audio duration from AudioProcessor instead of wall-clock time
- Update TranscriptionResult.Duration to report audio duration

Fixes #5

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
